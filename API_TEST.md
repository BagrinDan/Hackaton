# API Integration Test Script

## Purpose

The script `api_test.py` performs an end‑to‑end integration test of the Hackaton island services. It exercises every documented HTTP endpoint (GET, POST, DELETE, SSE) through the central API gateway, checks that the services respond with the expected status codes, and validates error handling paths.

## Gateway

All requests are sent to the gateway at:

```
http://localhost:8000/api
```

Each service is accessed under its own sub‑path (e.g. `/airport`, `/hotel`).

## Dependencies

- Python 3.12+
- `requests` – for regular HTTP calls
- `httpx` – for Server‑Sent Events (SSE) streaming

Install them with:

```
pip install requests httpx[sse]
```

## Running the Tests

```
python3 api_test.py           # normal, concise output
python3 api_test.py --verbose # detailed request/response logging
```

The `--verbose` flag prints the full request payload and a truncated response body for each call, which is useful for debugging.

## Test Overview

### 1. Gateway health
- **GET** `/health`
- Expects HTTP 200.

### 2. Broadcast Service
- **SSE** `GET /broadcast/events` – reads the first event.
- **POST** to a collection of placeholder endpoints (e.g. `/airport/arrival`, `/hotel/confirm`).
- Logs status; server errors (5xx) are highlighted.

### 3. Airport Service
- **GET** `/airport/health` – health check.
- **POST** `/airport/arrivals` – creates a guest arrival (valid payload).
- **POST** with an incomplete payload – expects HTTP 400.
- **GET** `/airport/arrivals/<guest_id>` – retrieves the created arrival.
- **GET** `/airport/arrivals/does-not-exist` – expects HTTP 404 (error path).
- **GET** `/airport/queue` – queue status.
- **GET** `/airport/stats` – summary statistics.

### 4. Hotel Service
- **GET** `/hotel/health` – health check.
- **POST** `/hotel/reservation` – creates a reservation (valid payload).
- **GET** `/hotel/reservation/<id>` – fetches the newly created reservation.
- **GET** `/hotel/rooms` – lists available rooms.
- **GET** `/hotel/reservation/by-guest/<guest_id>` – active reservation for a guest.
- **DELETE** `/hotel/reservation/<id>` – cancels the reservation.
- **POST** with an invalid payload – expects HTTP 400.
- **GET** `/hotel/reservation/nonexistent-id` – expects HTTP 404.
- **POST** with an unknown `room_type` – expects HTTP 400.

### 5. Beach Service
- **GET** `/beach/health` – health check (any 2xx is acceptable).
- **GET** `/beach/activities` – retrieves the list of activities.
- **GET** `/beach/activity/<activity_id>` – details for the first activity.
- **POST** `/beach/activity/book/<activity_id>` – books the activity.
- **POST** `/beach/activity/cancel/<activity_id>` – cancels the booking.
- Additional re‑book attempt logs the status.

### 6. Parrot Service
- **GET** `/parrot/health` – health check.
- **POST** `/parrot/chat` – simple anonymous chat request.
- **POST** `/parrot/chat` with a guest ID – triggers tool calls.
- **POST** `/parrot/chat/stream` – SSE streaming of chat responses.
- **GET** `/parrot/history/<guest_id>` – retrieves chat history.
- **GET** `/parrot/admin/metrics` – read‑only admin metrics.

## Error Verification

The helper `expect_error` is used throughout the script to confirm that error responses contain an `error` field in the JSON body and that the HTTP status matches the expectation (e.g., 400 for bad requests, 404 for missing resources).

## Extending the Tests

To add new service checks, follow the existing pattern:
1. Define the request using the `request` helper.
2. Use `report` for success expectations or `expect_error` for error paths.
3. Add a descriptive log entry with `log`.

All new tests will automatically respect the `--verbose` flag.

---
*Generated by the automated test suite documentation generator.*